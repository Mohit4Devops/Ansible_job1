---
  - hosts: mywebservers
    become: yes
    tasks:
    - name: install Java on ubuntu node
      apt: 
       name: "{{Ubuntupkg}}"
       state: present
      when: ansible_os_family=="Debian" 

    - name:
      yum:
       name: "{{redhatpkg}}"
       state: present
      when: ansible_os_family=="RedHat"
      

